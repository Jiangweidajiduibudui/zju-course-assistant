# 03 · 数据接口契约

- **版本**:v2.0(2026-07-08)
- **权威范围**:zdbk 与 chalaoshi 的端点/方法/参数、实测字段与语义、示例响应要求、缓存与刷新策略、异常处理、只读白名单与写接口黑名单、最近核验日期。
- **总原则**:本文所有内容以**真机抓包/实测为准**;公开项目(Lazuli/Quantum)推断只能作为交叉参考,不能替代实测与固定样例。字段未经核验前,**不能凭名称直接解释语义**。

---

## 1. 读写边界(advise-only 铁律)

- **只读白名单**:仅允许调用本文档明确列出的读接口。
- **写接口黑名单**:**禁止调用选课、退选、调序等一切业务写接口**。实现层面采用白名单策略——任何不在白名单内的端点(尤其 POST 修改类)一律禁止;新增读接口须先补入本契约。
- 此边界不因 D09(允许页面注入)而放松。

## 2. zdbk 接口(教务 jwglxt,选课模块 xsxk)

- **Base**:`https://zdbk.zju.edu.cn/jwglxt`
- **鉴权**:复用浏览器 zdbk 登录 cookie(host_permissions + credentials),由 service worker 后台 AJAX 调用。
- **模块参数**:`gnmkdm=N253530`(2026-07 登录态验证有效)、`su=<学号>`。
- **最近核验**:2026-07-08(登录态;**非选课期**,选课期行为须窗口期复核)。

### 2.1 端点清单(只读白名单)

| 用途 | 端点(相对 `/jwglxt`) | 方法 | 状态 |
|---|---|---|---|
| 学生信息/入口 | `/xsxk/zzxkghb_cxZzxkGhbIndex.html` | GET | 当前存在(2026-07 实测) |
| 课程列表 | `/xsxk/zzxkghb_cxZzxkGhbKcList.html` | POST | 当前存在(2026-07 实测) |
| 教学班列表 | `/xsxk/zzxkghb_cxZzxkGhbJxbList.html` | POST | 当前存在(2026-07 实测) |
| 已选/预置 | `/xsxk/zzxkghb_cxZzxkGhbChoosed.html` | POST | 当前存在(2026-07 实测) |
| 课程简介 | `/xsxk/zzxkghb_cxKcjj.html`,参数 `kcdm` | GET | **当前入口(2026-07 实测)**;旧入口 `/xkjjsc/kcjjck_cxXkjjPage.html` 已废弃,不得作为契约 |

> ⚠️ 选课期才返回完整数据;所有端点须在选课窗口期内**再次抓包复核**并留存固定样例(见 §5),此为进入实现的必做项(06 · F 记录)。

### 2.2 关键参数与字段(语义 + 核验状态)

**课程列表(KcList)**:入参 `nj`(年级)、`zydm`(专业代码)、类别(`dl`/`lx`)、`xkmc`(课名关键词,可选)。出参:

| 字段 | 语义 | 核验状态 |
|---|---|---|
| `kcdm` | 课程代码 | 已实测 |
| `xkkh` | 选课课号(教学班唯一标识) | 已实测 |
| `kcmc` | 课程名称 | 已实测 |
| `kkxy` | 开课学院 | 已实测 |
| `kcxz` / `kcxzzt` | 课程性质 / 选中状态 | 已实测 |
| 学分字段 | **复合字段,须字段核验后再解释,不得凭名称臆断** | ⚠️ 待核验 |

**教学班列表(JxbList)**出参:

| 字段 | 语义 | 核验状态 |
|---|---|---|
| `jsxm` | 教师姓名(`<br>` 分隔多师) | 已实测 |
| `sksj` / `skdd` | 上课时间 / 地点(时间为位域编码:学期+星期+单双周+节次) | 已实测(编码解析规则须固定样例验证) |
| `rs` | **"余量/容量"样式的复合字段;拆分规则待核验** | ⚠️ 待核验 |
| `yxrs` | 疑似"本专业待选~总待选";**语义待核验** | ⚠️ 待核验 |
| `kssj` | 考试时间 | 已实测 |
| `jxfs` / `skxs` | 教学方式 / 授课形式 | 已实测 |
| `gjhkc` | 国际化课程标记 | 已实测 |
| 志愿相关字段 | 页面暴露 3 个志愿位;分组可能由服务器字段动态决定(见 [02](02-浙大选课规则核对档.md) R-07/R-08/O-03) | ⚠️ 待核验 |

### 2.3 课程分类

**分类必须动态读取,不得硬编码**:分类集合(培养方案分类、通识各类等)随学期/政策变化,2026-07 实测已与旧文档枚举不同。实现:运行时从选课页面/接口读取分类清单;"选课搜索引擎"入口(页面内 `id=searchTool`,`data-dl=xk_9`,2026-07 实测存在)作为自由搜索通道。

### 2.4 异常处理契约

| 情形 | 识别 | 行为 |
|---|---|---|
| 登录失效 | 响应重定向至 CAS/登录页或特征状态码 | 通知 UI"请先登录";不重试轰炸 |
| 非选课期 | 接口返回空/特征提示 | 明示;信息类功能不受影响 |
| 空数据 | 正常响应但列表为空 | 如实展示"无数据",不当作错误 |
| 接口变更 | 字段缺失/解析失败 | 明示"数据获取失败(接口可能已变更)";上报待人工核查;**不静默吞错** |

### 2.5 缓存与刷新

- 课程/教学班列表:session 内短缓存;**生成推荐、重新优化、最终确认前强制刷新**余量、待选人数与正式状态,并显示更新时间(D20)。
- 已选/预置:进入 session、基线同步、一致性检查时拉取。

## 3. chalaoshi 接口

- **最近核验**:2026-07-08(实测,下表逐项标注)。
- **域名与可用性风险**:chalaoshi 官方 README 声明 **`chalaoshi.de` 已被墙,建议科学上网**;另提供 arpa 备用域名(可用性无保证)。→ 产品必须把"chalaoshi 不可达"当作**常态场景**设计(缓存优先、明确降级,见 [01-PRD](01-产品需求文档PRD.md) FR-12);域名应做成可配置项。
- **访问策略**:低频 + 本地缓存,避免高频触发 Cloudflare。

### 3.1 端点清单

| 数据 | 端点 | 说明 | 核验状态(2026-07-08) |
|---|---|---|---|
| 评分+热度+学院索引 | `https://chalaoshi.de/static/json/search.json` | 全校一份,~1.05 MB;`teachers[{id,name,py,sx,xy,hot,rate}]` + `colleges[{id,name}]` | ✅ 200 可用(实测) |
| 教师详情(按课均绩+点名比例+评分人数) | `https://chalaoshi.de/teacher/<id>/`(HTML) | 服务端直出;均绩按课程分行(如"微甲Ⅰ 3.97/500+"),含"XX% 认为会点名" | ✅ 可读取(实测) |
| 近五年评价 | `https://api.chalaoshi.de/comments/<teacherId>`(`sort=time` 可用) | **直接返回评论 HTML 片段**(含赞踩数+发布日期),无需会话 | ✅ 实测直接返回 |
| ~~`/getsession`~~ | — | **已废弃**:`api.chalaoshi.de/getsession` 当前 404,**不得写成必要步骤** | ❌ 404(实测) |

### 3.2 缓存策略(D28)

- **教师索引(search.json)**:随插件版本打包,或由后台机制定期更新。
- **教师详情与评论**:用户点开时按需拉取,本地缓存 **3 天**。
- **展示要求**:显示数据更新时间;支持强制刷新;使用过期缓存时**显著标记**。

### 3.3 数据合规立场(D03,权威决策见 06)

事实基础(来自 chalaoshi 官方仓库 README,2026-07-08 读取):**评论作者保留其内容的全部版权**,站方仅获得永久、不可撤回、非独占的许可。因此:

- 本项目**不宣称拥有数据**;不得使用"chalaoshi 没有数据版权"或"其声明必然无效"之类表述;
- 明确标注数据来源,始终保留原页链接;
- 提供**投诉、删除和停止收录机制**(渠道:本项目自身反馈入口;chalaoshi 侧可引导其官方 GitHub Issues / 邮箱);
- 继续离线打包教师索引并缓存评论,**不把取得站方授权作为上线前置条件——此为已接受的已知合规风险**(产品负责人决策,复审条件见 06 · D03)。

## 4. 隐私与脱敏

- 所有留存的示例响应/固定样例必须**删除个人信息**(学号、姓名、班级名单等)。
- zdbk cookie 只在浏览器内使用,不落盘导出、不进日志、不进提示词(见 04 §隐私)。

## 5. 固定样例(fixtures)要求

进入实现后,每个白名单端点必须留存:

1. 正常响应样例(脱敏);
2. 空数据样例;
3. 未登录/失效样例;
4. (选课期)含志愿字段的教学班样例——用于核验 §2.2 全部"待核验"字段。

样例入库 [05-测试计划](05-验收与测试计划.md) §2 所述 fixtures 目录,并回填本文档"核验状态"列与核验日期。
